VITE_API_URL=https://centrion-backend-gbe4a7a7d2h5dkde.canadacentral-01.azurewebsites.net
# TODO: set the deployed SAR (Annual Report) service URL before shipping the
# cycles feature — otherwise it falls back to http://127.0.0.1:8010 at runtime.
# VITE_SAR_URL=https://<sar-service-url>
# TODO: deployed SAR workspace app URL for PM/department_user login redirect.
# Falls back to http://localhost:3000 at runtime if unset.
# VITE_SAR_APP_URL=https://<sar-app-url>
